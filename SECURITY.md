# 보안 취약점 신고

**공개 이슈로 올리지 마세요.** 저장소의 **Security** 탭에서 *Report a vulnerability*로
비공개 신고할 수 있습니다.

받는 즉시 확인하고, 재현되면 수정 계획과 함께 회신합니다. 이 저장소는 태그를 따로
두지 않으므로 수정은 `main`에 반영합니다.

## 신고 대상

- 인증·인가 우회 (JWT 검증, 역할 기반 접근 제어)
- 다른 사용자의 대화나 세션에 접근
- 비밀번호·시크릿·API 키가 로그나 응답에 노출

## 신고 대상이 아닌 것

기본 구성은 개인 PC에서 시험용으로 쓰도록 맞춰져 있습니다. 아래는 의도된 기본값이라
신고하지 않으셔도 됩니다. 전체 목록과 잠그는 방법은
[INSTALL.md — 알아둘 제약](INSTALL.md#알아둘-제약)에 있습니다.

- **`.env.example`에 자격증명이 그대로 적혀 있음** — 바로 띄워볼 수 있게 채워둔 공개값입니다
- HTTPS가 아닌 평문 HTTP로 동작
- Keycloak이 개발 모드(`start-dev`)로 동작
- 게이트웨이·데이터베이스 포트가 호스트의 모든 인터페이스에 열림

Keycloak·LiteLLM·Next.js 같은 구성요소 자체의 취약점은 각 프로젝트로 신고해 주세요.
다만 이 저장소가 그것들을 안전하지 않게 엮어 쓰고 있다면 그건 여기로 알려주세요.
